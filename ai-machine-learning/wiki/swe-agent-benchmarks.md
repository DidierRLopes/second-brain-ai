# SWE-Agent Benchmarks

Software engineering agent benchmarks have evolved rapidly from small, Python-only evaluation sets toward large multilingual benchmarks, scalable procedural training environments, and hybrid verification schemes. The three papers covered here address three different bottlenecks: data scarcity for training (SWE-smith), environment quality and inference-time scaling (R2E-Gym), and language coverage (Multi-SWE-bench). Together they define the frontier infrastructure for building and evaluating [[llm-agents]] that autonomously resolve GitHub issues. The dominant training setup is an [[agent-harness-engineering|agent harness]] wrapping a ReAct loop (bash terminal + file editor + search), supervised-fine-tuned on expert trajectories generated by frontier models such as Claude 3.7 Sonnet, then evaluated on held-out SWE-bench Verified instances.

## R2E-Gym: Procedural Environments and Hybrid Verifiers

R2E-Gym (Jain, Singh et al., UC Berkeley / ANU, 2025) is the largest procedurally-curated executable gym for training SWE-agents, with 8,135 problems spanning multiple Python repositories, each equipped with executable environments, unit tests, and natural-language task descriptions. The project page is at r2e-gym.github.io.

### SweGen: synthetic data without human-written issues

The key obstacle to scale in prior work was reliance on human-written GitHub issues and human-written unit tests — both of which cap the number of training environments. R2E-Gym introduces **SweGen**, a synthetic data curation recipe that generates problem environments directly from commits via backtranslation and test generation:

1. Crawl Python repositories on GitHub, extract commit history and associated code changes, filter for "interesting" commits using rule-based and LLM-based heuristics.
2. For each relevant commit, collect build scripts and Fail→Pass (F2P) test cases — tests that fail on the buggy commit and pass after the fix.
3. When no associated tests exist, automatically generate them, reducing reliance on human-written test cases entirely.
4. Apply backtranslation to generate problem statements from the F2P test output rather than from human-written issues, producing precise and directed natural-language descriptions.

SweGen produces 2.5× more executable environments than methods that rely on GitHub issues, and models trained on synthetic problem statements (27.8% Pass@1) perform nearly identically to those trained on real issues (28.0%), validating the approach.

### Trajectory collection and SFT results

For training, R2E-Gym uses a minimal scaffold on top of OpenHands with a ReAct framework equipping the LLM with a bash terminal, file editor, and search tool. Trajectories are collected using Sonnet-3.5-v2 on a decontaminated subset of 4,578 environments (R2E-Gym-Subset) that has no repository overlap with SWE-Bench. From 3,321 successful trajectories across 2,048 unique task environments, the paper trains Qwen-2.5-Coder models at 7B, 14B, and 32B scale. The 32B model achieves **34.4% Pass@1 on SWE-Bench Verified**, an improvement of +13.8 percentage points over the SWE-Gym baseline (20.6%) at the same model size. Performance improves with more trajectories up to ~3,200 samples, with the 14B model saturating around 800 samples. Including agent thoughts in training targets (ReAct reasoning traces) is important: 34.4% vs. 30.4% without thoughts.

### Hybrid inference-time scaling

R2E-Gym provides the most thorough analysis to date of inference-time scaling for SWE-coding agents, comparing two complementary verifier axes:

**Execution-based (EB) verifiers** train a specialized testing-agent (Qwen-Coder-32B) to generate M=10 diverse test cases that evaluate whether a patch resolves the issue. A regression test filter then eliminates patches that fail existing tests. The EB score is the sum of passing tests, gated by regression compatibility. Limitation: for the majority of problems (>80%), fewer than 20% of tests provide discriminative signal because many tests either do not reproduce the bug or fail on the correct solution. Up to 10% of test cases are "toxic" — they pass incorrect patches and fail correct ones.

**Execution-free (EF) verifiers** fine-tune a Qwen2.5-Coder-14B model to output YES/NO tokens on the full trajectory (thoughts + patch), scoring by the normalized log-probability of YES. EF verifiers offer better discrimination across patches via a continuous score but are biased toward stylistic heuristics — attention analysis shows verifiers disproportionately attend to agent thoughts ("great, this is fixed!") rather than the actual patch content. Best@26 drops from 42.8% to 37.6% when agent thoughts are removed from EF verifier input.

**Hybrid verifier**: combining both by first filtering on EF top-n scores, then applying EB scores, the plateau around 42-43% for each individual method is broken. The hybrid achieves **51% Best@26 on SWE-Bench Verified** — a new state-of-the-art for open-weight SWE-agents and competitive with proprietary systems like o1 and Sonnet-3.5 w/ tools. Increasing the number of test-agent rollouts also improves performance more cost-efficiently than increasing editing-agent rollouts.

## SWE-smith: Scaling Data for Software Engineering Agents

SWE-smith (Yang, Lieret, Jimenez et al., Stanford / Princeton, 2025) addresses the data bottleneck directly: existing SWE training datasets have at most 1,000s of instances from 11 or fewer repositories, each requiring hundreds of hours of human labor and terabytes of storage. SWE-smith introduces an automated pipeline that generates 100–1,000 task instances per Python repository, producing a dataset of **50k instances from 128 GitHub repositories** — an order of magnitude larger than all prior work.

### The SWE-smith pipeline

The key design inversion relative to SWE-bench: define the execution environment first, then synthesize tasks within it. Given any repository, SWE-smith:

1. **Builds execution environments** by running SWE-agent on the latest commit for at most 100 steps, instructing it to install the codebase and run the test suite. Repositories targeting the 5,000 most-downloaded PyPI packages (sorted by GitHub stars) are filtered to those where >80% of existing tests pass; a Docker image is created.
2. **Generates bug candidates** using four strategies per repository:
   - **LM Modify** (56% yield, 17,887 instances, $0.38/candidate): provide the LM with a function and ask it to introduce an errant modification.
   - **LM Rewrite** (35% yield, 4,173 instances, $3.93/candidate): given only the function header and docstring, ask the LM to reimplement it.
   - **Procedural Modification** (40% yield, 15,641 instances, $0.00/candidate): AST-based random transformations (remove a conditional/loop, change an operator).
   - **Invert PRs / PR Mirror** (33% yield, 2,344 instances, $5.53/candidate): undo existing PRs to create bugs that mirror real development.
3. **Validates candidates** by applying each patch, running the test suite, and keeping only patches that break one or more previously-passing tests (Fail-to-Pass, F2P). Average cost: $2.32 per validated candidate, 6 median F2P tests, 5 median lines edited.
4. **Generates realistic issue descriptions** using an LM given the diff, failing test source code, and test execution output — this produces issue text comparable in quality to real GitHub issues for training purposes.

Total cost to create the 50k dataset: ~$1,360 ($1,000 for bug generation, $160 for automatic repository installation, $200 for issues on 10K bugs). Creating ~20h of human labor total — compared to hundreds of hours for prior datasets. Storage: 295 GB (vs. 50–150 TB to create the same quantity with SWE-bench methods, a ~500× difference).

### Training results and scaling behavior

Using SWE-smith, the paper trains `SWE-agent-LM-32B` by fine-tuning Qwen 2.5 Coder Instruct 32B on 5,016 expert trajectories generated by Claude 3.7 Sonnet (using SWE-agent for at most 75 steps and $2.00 cost limit per instance). Results on SWE-bench Verified:

| Model | System | Train Size | Verified |
|---|---|---|---|
| GPT-4o | SWE-agent | — | 38.8 |
| Claude 3.7 Sonnet | SWE-agent | — | 58.2 |
| R2E-Gym-32B | OpenHands | 3.3k | 34.4 |
| SWE-agent-LM-7B | SWE-agent | 5k | 15.2 |
| **SWE-agent-LM-32B** | **SWE-agent** | **5k** | **40.2** |

Key scaling findings:
- Performance improves monotonically with more trajectories (14.3% → 27.8% → 33.4% → 40.2% at 100 / 400 / 1,600 / 5,000 trajectories).
- Performance improves logarithmically with more repositories in training (10.3% → 11.5% → 12.9% → 15.1% at 5 / 25 / 50 / 100 repositories for 7B models on 700 procedural trajectories).
- Repository specialization significantly boosts target-repository performance with minor generalization loss (42.4% vs. 33.3% on SymPy in specialist vs. base 32B settings).
- Task difficulty (easy/medium/hard) correlates with solvability but not with effectiveness as training data — pass@1 scores for models trained on easy/medium/hard difficulty subsets are 12.4%, 10.8%, 13.6%, 12.2% (no strong correlation).
- **Repetitive actions are a key failure mode**: >25% of SWE-agent-LM-32B trajectories have repetitive sequences of length ≥10 (vs. <4% for Claude 3.7 Sonnet). Localization failures — the agent getting stuck during search or reproduction — account for 53% of the fine-tuned model's failures.

## Multi-SWE-bench: Multilingual Issue Resolving

Multi-SWE-bench (ByteDance Seed, 2025) addresses the monolinguality problem: SWE-bench focuses almost exclusively on Python, leaving the performance of LLMs on Java, TypeScript, JavaScript, Go, Rust, C, and C++ largely unmeasured. Multi-SWE-bench provides **1,632 human-validated GitHub issues spanning 7 programming languages** across 39 repositories.

### Benchmark construction

The pipeline has five phases:

1. **Repository selection**: GitHub repositories with 500+ stars, active maintenance for ≥6 months, CI/CD support (`.github/workflows/`), and buildability on the latest commit.
2. **Pull request crawling**: filter for PRs that (a) link to at least one GitHub issue, (b) modify test files, and (c) are merged to the main branch.
3. **Environment determination**: automatically extract repo-common and PR-specific dependencies, generate a tailored Dockerfile, iteratively patch on build failure, and verify the repository launches successfully.
4. **PR filtering**: run the full test suite under three settings (Run.log, Test.log, Fix.log) and filter for PRs with at least one ANY→FAILED→PASSED transition and no ANY→PASSED→FAILED regressions. This yields 2,456 instances.
5. **Manual verification**: 68 expert annotators (≥2 years experience in the target language, bachelor's degree or higher) independently label each instance, then cross-review. A 14-person internal QA team verifies annotator accuracy reaches ≥80%. Final retained: **1,632 high-quality instances**.

### Language distribution and difficulty

Multi-SWE-bench spans 39 repositories across Java, TypeScript, JavaScript, Go, Rust, C, and C++. Difficulty is measured by estimated time to resolve: easy (≤15 mins), medium (15 mins–1h), hard (≥1h). Key characteristics:

- Java and Rust issues tend to have the longest, most detailed descriptions (e.g., elastic/logstash averages 1,600 tokens per issue description).
- JavaScript, Go, and C issues are typically brief and focused.
- Rust and C++ patches often require large-scale edits (some modifying 200+ lines and 7+ files); JavaScript and TypeScript patches tend to be atomic (under 3 hunks and fewer than 2 files).
- Resolved rates drop sharply when fix patches exceed 600 tokens or touch more than one file, exposing weaknesses in long-context retention and multi-file reasoning.

### Evaluation results

Using Claude-3.5-Sonnet with three representative methods (Agentless, MSWE-agent, MOpenHands):

| Language | Best resolved rate (%) |
|---|---|
| Python | ~42 |
| Java | ~20 |
| TypeScript | ~8–12 |
| JavaScript | ~8 |
| Go | ~12 |
| Rust | ~5–7 |
| C | ~12 |
| C++ | ~12 |

Python performance substantially outperforms all other languages. Models perform generally better when issue descriptions are longer (more contextual grounding), and harder instances show lower resolve rates across all languages and methods. Multi-SWE-bench also launches the **Multi-SWE-RL** open-source community, releasing 4,723 containerized instances for RL training across 7 languages as a community-driven effort toward scalable RL in software engineering.

## Comparison

| Benchmark / Dataset | Size | Languages | Executable envs | Source | Notes |
|---|---|---|---|---|---|
| SWE-bench Verified | 500 | Python | Yes (Docker/PR) | Real PRs | Human-curated holdout |
| SWE-gym | 2.4k | Python | Yes | Real PRs | 6 TB storage |
| R2E-Gym | 8.1k | Python | Yes | Commits + synth | 4 TB subset; SweGen backtranslation |
| SWE-smith | 50k | Python | Yes | Real repos + synth bugs | 295 GB; 128 repos |
| Multi-SWE-bench | 1.6k | 7 languages | Yes (Docker) | Real PRs + manual verify | 68 expert annotators |

The critical infrastructure insight: R2E-Gym and SWE-smith both demonstrate that synthetic problem statements match the training value of real GitHub issues while providing dramatically better scalability. The bottleneck for the next generation of SWE-agents has shifted from eval quality to training environment diversity and multi-language coverage.

## Related Topics

- [[llm-agents]] — ReAct loop, planning, and tool use that underlies all SWE-agent scaffolds
- [[agent-harness-engineering]] — Production harness design, error taxonomies, context strategies, and evaluation metrics that apply directly to SWE-agent pipelines
- [[reasoning-models]] — The frontier models (Claude 3.7 Sonnet, GPT-4o, DeepSeek-R1) that generate expert trajectories and serve as SWE-agent backbones

## Sources

- [R2E-Gym: Procedural Environments and Hybrid Verifiers for Reinforcement Learning Agents (2504.07164)](../../papers/07-applications/agents-swe/R2E-Gym: Procedural Environments and Hybrid Verifiers for Reinforcement Learning Agents - 2504.07164.pdf) — 8.1k SweGen environments, hybrid verifier (51% SWE-Bench Verified), synthetic vs. real problem statement ablation.
- [SWE-smith: Scaling Data for Software Engineering Agents (2504.21798)](../../papers/07-applications/agents-swe/SWE-smith: Scaling Data for Software Engineering Agents - 2504.21798.pdf) — 50k instances from 128 repos, SWE-agent-LM-32B achieves 40.2%, logarithmic scaling with repositories.
- [Multi-SWE-bench: A Multilingual Benchmark for Software Engineering (2504.02605)](../../papers/07-applications/agents-swe/Multi-SWE-bench: A Multilingual Benchmark for Software Engineering - 2504.02605.pdf) — 1,632 human-verified instances across 7 languages, Multi-SWE-RL community dataset.
